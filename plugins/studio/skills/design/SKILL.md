---
name: design
description: Нужен макет экрана — короткая форма /order design. Собрать бриф по docs/orders/design.md и docs/DESIGN.md, выгрузить дизайн-систему проекта в Claude Design, записать долг. Команда чата замысла.
argument-hint: "<экран>"
---

Нужен макет экрана: **$ARGUMENTS**

Это короткая форма `/order design <экран>`. Прочитать `../order/SKILL.md`
относительно базовой папки этого скилла и выполнить его целиком с видом
`design` и описанием выше. Бриф собирать с **настоящими данными**: самый
короткий и самый длинный текст, ноль и сто элементов — а не с заглушками.
