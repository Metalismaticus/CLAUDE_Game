# Проверка работоспособности

**Состояние полигона: {{нет | строится | есть}}**

Пока состояние не «есть», одобрение `reviewer` значит только то, что названо в
строке «Что проверяется уже сейчас». Он обязан писать это в итоге, а `/start`
— в отчёте владельцу.

## Команды проверки

Единственное место, где записано, **чем** проверять. Команды `/start`, `/done`,
`/check` и субагенты берут их отсюда, а не из памяти.

| Уровень | Команда | Сколько идёт · предел | Как читать вердикт |
|---|---|---|---|
| быстрая — продукт запускается без ошибок | {{…}} | {{замер · предел = замер × 3}} | {{код возврата честный? если нет — по каким словам в выводе}} |
| проверка пункта — одна по имени | {{…}} | | |
| группа | {{…}} | | |
| полная | {{…; у игры с раннером — и все сценарии игрока; без группы «замеры»}} | | |
| долгая — не входит в полную | {{… или «нет»}} | | |
| замеры — группа «замеры», в конце партии | {{… или «нет»}} | | {{код 0 / 1; 3 — «замер не состоялся: <причина>», не красное}} |
| снимок экрана | {{… или «нет»; как задать место: экран, seed, координаты, ракурс}} | | |
| снимок кадра — стенд вида | {{команда вида `снимок кадра <имя> [вариант] [--seed … --место …]` — или «нет»}} | | {{объект не в кадре — ошибка, а не снимок}} |
| стенд ощущения — площадка для владельца | {{запуск отладочной сборки на площадке с пресетами темы: `стенд ощущения <тема> [--quit-after N]` — без N окно владельцу, с N закрывается само (агентам — только так) — или «нет»; без управления — «не нужен»}} | | {{окно открылось, на экране надпись варианта}} |
| сценарий игрока — один по имени | {{запуск `tests/scenarios/<имя>.json` — или «нет»; не игра — «не нужен»}} | | код 0 / 1 / 2 — «Сценарии игрока» |
| после правки ресурсов | {{переимпорт, сборка, генерация — или «не нужно»}} | | |
| сборка — версия для игроков | {{для Godot `--headless --export-release "<пресет>" <путь>` — или «нет»}} | | {{по чему судить: файл появился, слова в выводе}} |
| проверка сборки — собранная, не редактор | {{запуск собранного exe на N кадров или сценарий игрока на сборке — или «нет»}} | | {{честный вердикт: по каким словам вывода}} |

Канонические ракурсы — что снимается у любого пункта про вид помимо места из
«Как увидеть»: {{места и размеры экрана — или «нет интерфейса»}}.

Как открывается окно — правило для всех команд, которые пишут `/setup` и
исполнители: окно проверки не мешает владельцу работать или играть.
{{Проверки логики — без окна (Godot `--headless`). Снимки, стенд, сценарии с
рендером, которые агенты запускают (`--quit-after N`), — маленькое окно в
углу, где оно не закрывает панель задач, без фокуса, не поверх всех и без
звука (Godot: `--position <x,y> --resolution <w×h> --audio-driver Dummy`);
снимок полного размера — из вьюпорта заданного размера (stretch `viewport`
или SubViewport), а не из окна; без фокуса — только настройкой
`display/window/size/no_focus` до создания окна и только у проверок (Godot:
`application/config/project_settings_override` = `user://checks_override.cfg`,
файл лежит лишь в профиле user:// проверок — свой `APPDATA` у их скриптов;
`WINDOW_FLAG_NO_FOCUS` из скрипта опаздывает, `override.cfg` в корне
действует и на редактор, и на игру владельца); `always_on_top` — никогда.
Окно владельцу («стенд ощущения» без N, первое окно `/setup`) — обычного
размера, с фокусом и звуком. Замеры — видимое окно в фокусе (без
`checks_override.cfg`), только в конце партии по «да» владельца. Другой движок — его флаги; не умеет рисовать без
фокуса — так и записать; флаг, не проверенный запуском на 1 кадр, — «не
проверено: <флаг>».}}

В контекст чата брать только итоговую строку прогона, а не весь вывод.

## Что проверяется уже сейчас

{{что из таблицы реально работает сегодня}}

## Принято без полной проверки

Пополняет `/done` из строк `Ограничение:` и «вид не проверен»: пункт, дата,
чем ограничено. Запись снимается только ссылкой на проверку, которая это
покрыла.

## Нестабильные проверки

Проверка, красная 1–2 раза из 3 повторов, — сюда: имя, дата, как часто. Продукт
по ней вслепую не чинить — сначала сделать проверку устойчивой.

## Ловушки стека

Неочевидное, на чём уже теряли время. Пополняет `/done` из строк `Ловушка:`
итогов исполнителя; исполнитель читает раздел перед работой.

## Данные пользователя

{{сохранения, профили, базы, которые живут вне папки проекта и которые партия
может задеть: где лежат и как снять копию. «Нет» — если нет.}}

Агентам трогать их нельзя. `/start` снимает копию перед партией, которая может
их задеть.

## Полигон

{{как устроен набор проверок: где лежит, как называются проверки и группы,
чем проверка отличается от зонда-замера}}

### Правила каркаса

- проверка без единого утверждения — провал;
- ошибка скрипта во время проверки — провал, если её явно не разрешили;
- пропуск — только с причиной;
- пустой вывод или ненайденный движок — красное;
- итог перечисляет каждый провал поимённо;
- у каждой команды предел времени (замер × 3), зависание — красное (код 124).

### Как добавить проверку

{{шаги и образец; у игры — и как добавить сценарий игрока. Правило: новая
проверка должна быть красной без исправления и зелёной с ним — иначе она
ничего не стережёт.}}

### Красное, которое бывает правильным

Проверку заводят и на открытый баг — тогда она красная намеренно и стережёт
починку. Прежде чем «чинить красное», сверить с `BUGS.md`. Такую проверку
нельзя убирать или ослаблять ради зелёного итога.

### Чего проверки не видят

{{обычно — вёрстку и картинку: это ловится только снимком экрана. Дополнить
тем, что верно для этого продукта: звук, сеть, реальные устройства.}} Числа
стерегут числа, а не картинку и не ощущение: вид и ощущение решает владелец
выбором из вариантов («Стенд»).

## Сценарии игрока

Для игры; продукт не игра — раздел одной строкой «не нужен: <почему>».

Пункт про управление, интерфейс или действия игрока проверяется сценарием
через ввод: он нажимает, как игрок, а не вызывает функцию. Раннер сценариев —
часть полигона, команда — строка «сценарий игрока» в «Командах проверки».
Сценарий — файл данных `tests/scenarios/<имя>.json` {{или ресурс движка того же
смысла: …}}: точка входа (сцена или уровень), зерно, фиксированный шаг времени,
шаги. Пример (Godot):

```json
{
  "entry": "res://main.tscn",
  "seed": 1,
  "fps": 60,
  "steps": [
    {"press": "кнопка:Новая игра", "frame": 30},
    {"wait_for": "Player", "timeout": 120},
    {"press": "move_forward", "hold": 60},
    {"wait_frames": 70},
    {"assert": "Player.position.z", "max": -2.0},
    {"press": "jump", "hold": 10},
    {"assert": "Player.position.y", "min": 2.6, "within": 60},
    {"assert": "group:enemies.count", "min": 1},
    {"assert": "текст:Уровень 1", "equals": true},
    {"shot": "first-steps"}
  ]
}
```

- Время — в кадрах при фиксированном шаге (`fps`), не в секундах.
- `press` — действие ввода из настроек управления или `кнопка:<текст>`: кнопку
  интерфейса нажимают по тексту, не по координатам; `frame` — кадр от начала
  сценария (нет — сразу); `hold` — сколько кадров держать, следующие шаги
  идут, пока держится.
- `wait_for` — узел, объект или `текст:<строка>` на экране; не дождался за
  `timeout` кадров — провал.
- `assert` — по именам и группам объектов (`Player.position.z`,
  `group:enemies.count`) или `текст:<строка>` на экране; `equals`, `min`, `max`.
  Без поля — на текущем кадре; `"within": N` — зелено, если условие выполнилось
  хоть на одном из следующих N кадров (вершина прыжка); `"always": N` — на
  каждом из них (не падает, не уходит за край).
- `shot` — снимок кадра с этим именем в папку снимков, названную раннеру.
- Коды: 0 — зелено; 1 — провал: какое утверждение, на каком кадре, какие
  действия остались зажаты; 2 — плохой сценарий (нет файла, неизвестный шаг
  или действие).
- Полная проверка и группа `сценарии` прогоняют все `tests/scenarios/*.json`,
  провалы — поимённо: так сценарий ядра стережёт управление и от следующих
  пунктов.

«Готово, когда» пункта, записанное шагами игрока, — текст сценария: шаги и
числа берутся оттуда.

## Стенд

Место, где 2–4 варианта пункта показываются владельцу одинаково раз за разом,
чтобы он выбрал. Две части, у каждой своё состояние: «вид» — для `[вид]`
(стенд вида), «ощущение» — для `[ощущение]` (стенд ощущения). Пока нужная
часть не «есть», пункт начинается с её постройки. Ненужная часть — «не нужна:
<почему>»: вид — продукту без мира (только экраны), ощущение — без управления
и камеры.

### Вид

**Состояние: {{нет | строится | есть | не нужна: …}}**

Варианты снимаются рядом с образцом на одних и тех же кадрах:

- отдельная сцена или режим запуска: {{…}};
- именованные кадры: камера ставится по имени кадра из паспорта
  `docs/refs/<тема>.md` («Кадры»);
- время и ветер остановлены — два снимка одного кадра одинаковы;
- пресеты света: нейтральный и целевой;
- вариант включается аргументом (`A`, `B`, `C` …), пресет графики — тоже
  (`--пресет низкие|высокие`);
- для масштаба в кадре — капсула 1,8 м и куб 1 м;
- движок проверяет, что объект в кадре, иначе снимок — ошибка;
- место владельца снимается по seed и координатам из записи бага или «Как
  увидеть»: по клавише (F3) игра показывает seed, координаты и направление
  камеры и копирует их одной строкой, этой строкой стенд ставит камеру.

Команда — строка «снимок кадра» в «Командах проверки». Лист сравнения —
`python tools/look_sheet.py --ref <образец> --var A=<снимок> [--var B=…]
--out docs/refs/<тема>/sheet-<дата>.png --json <файл>` (нужна Pillow; код 2 —
её нет). Картинки образцов на месте — `python tools/refs_check.py`, код 0.

### Ощущение

**Состояние: {{нет | строится | есть | не нужна: …}}**

То, что судится игрой или на слух (управление, камера, темп и тайминг
анимаций, звук, отдача): владелец пробует варианты руками.

- тестовая площадка {{сцена или уровень}}: ступени и ящики известной высоты
  ({{0,25 · 0,5 · 1 · 2 м; у 2D — в клетках}}), край обрыва, узкий проход,
  стена для камеры, разметка метров на полу;
- в отладочной сборке пресеты темы, названной при запуске, переключаются
  клавишами 1–4 — действиями ввода {{`variant_1` … `variant_4`}}: сценарий
  нажимает их, как игрок; 1–4 заняты игрой (панель предметов) — {{F1–F4}},
  какие — в листе вариантов;
- на экране крупно — текущий вариант и его числа («Вариант B · прыжок 1,2 м
  · 0,35 с до верха»); сценарий видит его как `текст:Вариант B`;
- у стенда свои проверочные пресеты `_проба` A и B — на них его сценарий;
  клавиши, надпись и `_проба` остаются навсегда, встраивание убирает только
  пресеты своей темы;
- звук — источник звука темы по клавише и музыка фоном, чтобы громкость
  слышалась относительно неё; варианты — ещё и файлами
  `docs/refs/<тема>/variant-<дата>-<буква>.wav` (PCM 16 бит: у OGG
  `tools/asset_check.py` не видит пик, клиппинг и обрыв; Godot пишет `.wav`
  рядом с `--write-movie <файл>.png --quit-after N`), брак ловит
  `tools/asset_check.py`;
- в выпускной сборке переключателя и надписи нет.

Команда — строка «стенд ощущения» в «Командах проверки». Лист вариантов —
`docs/refs/<тема>/sheet-<дата>.md`: вариант, чем отличается, числа; как
запустить, какие клавиши, что попробовать.

## Параллельная работа

**Состояние: {{не проверено | проверено | невозможно}} · пишут: {{1}} · тяжёлых проверок: {{1}}**

`/start` делает независимые пункты одновременно, каждый в своей копии проекта
(git worktree), только при состоянии «проверено» и `пишут` больше 1. Решает по
пунктам разведчик `scout`; сомнение — по одному.

- `пишут` — сколько пунктов пишется одновременно: 0 или 1 — всё по одному; 2,
  3, 4 … — до стольких разом. `/parallel N`, на один запуск — `/start all xN`.
- `тяжёлых проверок` — сколько полных и долгих прогонов идёт одновременно,
  обычно 1: в `/start all` проверяющие пунктов работают в лёгком режиме, а
  полная и долгая идут один раз, в основной папке в конце партии.
  `/parallel проверки M`.
- Проверить числа пробным прогоном — `/parallel проверить`.

### Подготовка копии

Что сделать в свежей копии, прежде чем в ней работать и проверять. Свежая
копия — это только файлы из git: без установленных зависимостей, кэшей,
сгенерированных файлов и локальных настроек.

```bash
{{например: установить зависимости; собрать или импортировать ресурсы;
положить локальную настройку, которая отделяет данные копии}}
```

Готово, когда быстрая проверка в свежей копии зелёная — замер: {{дата, сколько
заняла подготовка}}.

### Набор копий

Копии — `../<папка проекта>.wt/slot<K>`, по одной на пишущий пункт. Первый
раз слот создаётся (`git worktree add -b wave/p<N> <slot> main`) и готовится
по «Подготовке копии», дальше переиспользуется между волнами партии:

```bash
git -C <slot> reset --hard
git -C <slot> clean -fd
git -C <slot> checkout -B wave/p<N> main
```

`clean` без `-x` не трогает файлы из `.gitignore` — подготовленные кэши,
зависимости и локальная настройка копии остаются, если они там; всё, что
кладёт подготовка, должно быть в `.gitignore`. Страж коммитов разрешает
`reset --hard` и `clean` только в копиях `*.wt/…`. Что повторить в
переиспользованной копии, если менялись зависимости или ресурсы: {{… или
«ничего»}}.

### Отдельные данные

{{как сделать, чтобы копия не делила с основной папкой и с другими копиями
то, что живёт вне git: сохранения и профили, базу, кэш, порт сервера, папку
логов. Каждая копия получает своё — по имени копии.}}

### Общие узлы

Файлы и системы, которые **никогда** не меняют два пункта одновременно: от них
зависит много кода, и слияние по ним молча ломает соседа.

- {{конфигурация проекта, схема данных и формат сохранения, миграции, общие
  реестры и базовые классы, таблицы локализации, файлы зависимостей и lock}}

### Нельзя одновременно

{{проверки, которые врут при параллельном прогоне: замеры времени и
производительности, проверки на общем порту или общей базе. Их гонять только
в основной папке и одну.}}

- снимки стенда — по одному;
- **группа «замеры»** — проверки полигона с порогом кадров или времени
  («p99 кадра ≤ 22 мс», «кусок строится быстрее 50 мс»): ни в копиях, ни в
  полной не запускаются; идут строкой «замеры» в конце партии, в основной
  папке и одни, после предпроверки и «да» владельца («Как мерить» в
  «Бюджете производительности»); пропуск пишется строкой «замеры пропущены
  — в конце партии», а не зелёным.

## Замеры

{{как снять число с работающего продукта: зонд, профилировщик, лог. Числа в
проекте получают прогоном, а не рассуждением.}}

Зонд, понадобившийся второй раз, коммитится как проверка или сценарий игрока.

## Бюджет производительности

{{Продукту без мира и анимации — одной строкой «не нужен: <почему>».}}
Судим по **времени кадра в мс** — p99 и рывкам; FPS — в скобках для
владельца. Коэффициенты машин — `tools/perf_ref.json`.

### Цели для игроков

Решение владельца (окно `/setup`); прежние его решения о скорости
(`docs/DECISIONS.md`, ограничения `docs/CONCEPT.md`) главнее умолчаний.
Источник: {{опрос Steam, август 2026 (store.steampowered.com/hwsurvey);
требования Minecraft Java 2026 (minecraft.net); Steam Deck и Steam Machine
Verified (partner.steamgames.com/doc/steamhardware/compat) — `sources`
`tools/perf_ref.json`}} · пересмотреть до {{<дата + год>}}.

| Уровень | Эталонное железо | Пресет | Разрешение | Цель: p99 кадра | Рывки |
|---|---|---|---|---|---|
| минимальный | {{GTX 1060 6GB / RX 580 · 4 ядра / 8 потоков · 8 ГБ · 4 ГБ видеопамяти}} | низкие | {{1080p}} | {{≤ 40 мс (среднее ≥ 30 FPS, 1% худших ≥ 25)}} | 0 после прогрева |
| рекомендуемый | {{RTX 3060 / RTX 4060 · 6–8 ядер · 16 ГБ · 8 ГБ}} | высокие | {{1080p}} | {{≤ 22 мс (среднее ≥ 60, 1% худших ≥ 45)}} | 0 |
| машина владельца | {{из «Окружения»}} | ультра | {{его экран}} | {{под частоту монитора: 120 Гц — ≤ 11 мс (среднее ≥ 120, 1% худших ≥ 90), где предел — видеокарта; где процессор — как рекомендуемый, ≤ 22 мс (среднее ≥ 60, 1% худших ≥ 45)}} | 0 |

Рывок — кадр дольше 2× бюджета уровня (30 FPS — 66,7 мс, 60 — 33,3, 120 —
16,7). Эталоны уровней — `levels` в `tools/perf_ref.json`, меняются вместе.

### Как мерить — группа «замеры»

- **Что мерить**: время каждого кадра; CPU рендера и GPU раздельно (Godot:
  `RenderingServer.viewport_set_measure_render_time(rid, true)`, затем
  `viewport_get_measured_render_time_cpu(rid)` + `get_frame_setup_time_cpu()`
  и `viewport_get_measured_render_time_gpu(rid)`; другой движок — узнать у
  движка, как он отдаёт время CPU и GPU кадра); наш код — своими таймерами. В
  итоге — p50/p95/p99, максимум, число рывков и предел: CPU или GPU.
  `Engine.get_frames_per_second()`, `TIME_FPS`, `TIME_PROCESS` и средний FPS
  в утверждения не брать: они за секунду, фриз в 100 мс в них не виден.
- **Предпроверка** — не прошла: «замер не состоялся: <причина>», код 3, не
  красное. Основная папка, не копия; других процессов движка нет; процессор
  свободен — ≤ 10% за 3 с без процессов сборки проекта (`dotnet`,
  `VBCSCompiler`, `MSBuild`; их сначала подождать, до 30 с); видеокарта
  свободна — сумма `engtype_3D` по процессам, кроме `dwm`, приложения Claude и
  самого замера, ≤ 5% за 3 с (Windows:
  `Win32_PerfFormattedData_PerfOS_Processor` и
  `Win32_PerfFormattedData_GPUPerformanceCounters_GPUEngine` — от языка ОС не
  зависят); питание от сети; окно видно и в фокусе (перекрытое Windows
  замедляет); vsync выключен; C# оптимизирован — экспортированная сборка
  (`ExportRelease`), а пока шаблонов экспорта нет — сборка редактора
  `dotnet build <csproj> -p:Optimize=true` (редактор грузит только Debug, а
  свойство из командной строки перекрывает `Optimize=false` Godot.NET.Sdk);
  конфигурация — в базу (`csharp: Debug+Optimize` | `ExportRelease`), разные
  не сравнивать. Шаблоны экспорта ставятся только по «да» владельца
  окном.
- **Прогон**: прогрев ≥ 5 с и пока растёт компиляция шейдеров (Godot —
  `Performance.PIPELINE_COMPILATIONS_*`; рендер без этих счётчиков, как
  `gl_compatibility`, — первый прогон целиком прогрев и выбрасывается, рывки
  — медиана 5 прогонов); маршрут — по кадрам (`--fixed-fps 60`), а не по
  реальному времени, ≥ 1000 кадров после прогрева; мерить **во время
  подгрузки** мира, а не после неё; разрешение — уровня (экран 1080p — во
  весь экран), иначе GPU-часть пересчитать по числу пикселей.
- **Пробный режим** (`--проба`: 1 прогон, 1 пресет, без предпроверки, порогов
  и базы, `--quit-after`) — им проверяющий убеждается, что пункт, менявший
  группу, не сломал её скрипт; числа пробы не судят.
- **Компьютер занят** (владелец играет или работает) — не мерить: «замер
  отложен — компьютер занят». В конце партии `/start` спрашивает окном,
  мерить ли сейчас (окно игры на ~N минут); «Позже» — строка в
  `docs/BATCH.md`, партия не краснеет. Зонд со временем кадра (проба стека,
  цена эффекта) — та же предпроверка; занят — «замер отложен», без числа.
- **Каждый пресет графики** — свой прогон и своя цель: низкие → минимальный,
  высокие → рекомендуемый, ультра (нет — самый высокий) → машина владельца.
  Настроек графики ещё нет — один прогон на цель рекомендуемого.
- `[вид]` и `[ощущение]`, у которых есть цена в кадре: исполнитель и
  проверяющий пишут, на каком пресете что проверено.

### Бюджет систем

На рекомендуемом уровне, мс на кадр ({{оценка до первого замера | замер <дата>}}):

| Система | мс | Чем мерить |
|---|---|---|
| наш код в главном потоке | {{…}} | свои таймеры |
| публикация и подгрузка (мир, ресурсы) | {{…}} | свой таймер |
| CPU рендера | {{…}} | Godot: measured_cpu + frame_setup |
| GPU | {{…}} | Godot: measured_gpu |

Первые три в сумме и GPU отдельно — ≤ ~80% бюджета (60 FPS — ≤ 13,3 мс):
процессор и видеокарта работают одновременно, кадр — по более медленному.

### Машины замера

| Машина (id) | Уровень | k_cpu 1 поток | k_cpu много | k_gpu | Порог CPU / GPU, мс | Источник |
|---|---|---|---|---|---|---|
| {{i5-12400_rtx-4060-ti}} | {{минимальный}} | {{1,44}} | {{2,22}} | {{3,21}} | {{23,2 / 10,4}} | {{таблица, оценка ±25% \| калибровка <дата>, измерено}} |

Отпечаток {{<id>}}: {{CPU, ядра/потоки, GPU, видеопамять, драйвер (у NVIDIA
— мажор: 32.0.15.9597 → 595), ОС, дисплей и частота, питание, версия
движка, рендерер}} — из `env_check.ps1` плагина и движка.

- **k** = показатель машины / эталона уровня — по `k_rule` таблицы: CPU —
  PassMark один поток (главный поток) и много потоков (фоновая работа,
  генерация мира), GPU — по каждому источнику; у CPU и GPU — наибольшее по
  эталонам уровня: порог строже.
- **Порог на машине** = цель p99 уровня / k / 1,2 — отдельно для CPU и GPU
  части кадра. Уровень «машина владельца» на ней же — k = 1, без запаса.
- **Калибровка** — тот же сценарий на эталонной машине: k — отношение
  медиан, строка «калибровка <дата>, измерено» заменяет оценку. Машина
  калибровки не эталон (ноутбук владельца) — k уровня = k измеренный
  (владелец / она) × k по таблице (она / эталон уровня), источник
  «калибровка <дата> + таблица, частично измерено». Долг калибровки — строкой
  «Не блокер» `docs/BLOCKED.md`: «калибровка на <машине>: `/check замеры` на
  ней, когда под рукой».
- **Имя → строка таблицы и id машины** — по `match_rule` `tools/perf_ref.json`
  (так же игра при автоподборе пресета). Нет в таблице (не ошибка) —
  добавить строку по PassMark с URL и датой, пометка «оценка»; до того пороги
  уровней на этой машине не проверяются («машина не в таблице»), регрессии по
  её базе — проверяются.

### Регрессии

База — `tests/perf/baselines/<id машины>/<сценарий>.json`: отпечаток, движок,
рендерер, сборка C#, пресет, окно, коммит, дата; 5 прогонов, по метрике —
медиана и разброс (MAD). **Красное** — только если медиана хуже базы больше
чем на max(10%, 3 × разброс, 0,25 мс) **и** повтор (ещё 5 прогонов) это
подтвердил. Базы нет — первый прогон группы её снимает. Отпечаток сменился
(железо, драйвер, версия движка, рендерер, сборка C#) — прогон сам снимает
новую базу, прежнюю хранит рядом (`<сценарий>.<дата>.json`), в итог —
«база переснята: <что сменилось>», не красное. При том же отпечатке
пересъём — только `/check замеры пересъём` по слову владельца, не сразу
после красного: иначе база узаконит регрессию.

## Выпуск

Для `/release`; продукт не выпускают сборкой — раздел одной строкой «не
нужен: <почему>».

- Пресеты экспорта: {{имя → платформа; у Godot — `export_presets.cfg`,
  шаблоны экспорта ровно под версию движка из «Окружения» | нет — до первого
  выпуска}}.
- Куда складывается сборка: {{папка, например `build/`, — в `.gitignore`}}.
- Площадки: {{itch.io — `<user>/<игра>:<канал>`; Steam — app id, depot,
  бета-ветка, файл app_build vdf рядом с папкой сборки | ещё не выбраны —
  спросит `/release`}}.

Сборка — строка «сборка», проверка **собранной** версии, а не редактора, —
строка «проверка сборки» в «Командах проверки». Вход в butler и SteamCMD
владелец делает сам; паролей, ключей и имени аккаунта Steamworks здесь не
писать. Заметки к выпускам —
`docs/releases/<версия>.md`.

## Окружение

Заполняет `/setup` проверкой компьютера; повторная проверка — тем же
способом. Версии — из установленного, не из памяти; чего не удалось
определить — так и пишется. Это факты, а не решения владельца.

Движок: {{путь к исполняемому файлу · версия · шаблоны экспорта: версия | нет
| не нужны}}. Заметки о версии движка: {{`docs/engine-notes.md` | не нужны}}.
Context7 — документация ровно этой версии: {{есть | нет — предложен <дата> |
не нужен: стек знаком}}.

| Что | Версия или значение | Где |
|---|---|---|
| {{компьютер: ОС, процессор, видеокарта и видеопамять, память, монитор и его частота, питание (сеть/батарея, режим), свободно на диске проекта}} | | |
| {{git (`core.autocrlf`, git-lfs), python и Pillow, dotnet, ffmpeg, Blender, gh, winget — что нужно стеку}} | | |

Поставлено: {{<дата> — <программа> через winget (`<id>`) или «путь указал
владелец»; пропущено — что без неё не заработает | ничего}}.

Первый запуск окна: {{<дата> — окно видно, снимок `docs/refs/setup/first-frame.png`
| не запустилось: <причина — кириллица в пути, консольная сборка, драйвер …>}}.
