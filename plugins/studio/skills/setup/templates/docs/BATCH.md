# Текущая партия

Файл ведёт чат разработки: `/start` записывает сюда снятый список, `/done`
очищает. Чат замысла его не правит — так два чата в одной папке не пишут в
один файл одновременно (`CLAUDE.md`, «Два чата»); замечание владельца о пункте
партии он пишет к пункту в `ROADMAP.md`.

Пусто.

<!-- Образец партии — шапка строками над таблицей:

Снята `/start …` <дата> с «Очереди» на <хеш>.
Данные пользователя: <не задевает | копия в …>.
Исходная проверка: <уровень> <зелёных>/<провалов> на <хеш>.
Последний полный прогон: <уровень> <зелёных>/<провалов> на <хеш> | не было
Волны (от `scout`): 1: [2, 3] — разные системы · 2: [1] — меняет формат данных.

| № | Волна | Пункт очереди | Критерий готовности | Состояние | Как увидеть | Решено за вас | В документы при `/done` |
|---|---|---|---|---|---|---|---|

Состояния: `ждёт очереди` · `в работе · круг R/3` · `готов к проверке ·
одобрен на круге R/3` · `ждёт: <вопрос>` · `не выполнен: <замечания>`.
Файл коммитится только отдельно, `Партия: …`, не в коммите `Пункт N:`.
«Ограничение» проверяющего и «Ловушка» исполнителя пишутся в «В документы при
`/done`» с префиксами `Ограничение:` и `Ловушка:`. -->

## Общий пробел

Сюда пишет `/start all`, когда два пункта споткнулись об одну конкретную вещь
или два подряд «не удалось»: «<что>; задело N, M». Похожие пункты ждут
исправления; после него сначала идёт один пункт.

## Вопросы владельцу

Вопросы агентов в `/start all` (блоки «Вопрос владельцу») копятся здесь и
задаются окнами в конце партии; неотвеченное остаётся здесь, в «Решения»
`BLOCKED.md` его переносит `/done`.

## Пришло — внести в очередь

Сюда пишет `/add`; переносит в `ROADMAP.md` и `BLOCKED.md` чат замысла.

## Найдено по ходу — внести в очередь

Сюда пишет координатор `/start`: что исполнители заметили вне своего пункта.
