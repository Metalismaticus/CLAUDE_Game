# Заказ: 3D-модель (`model3d`)

**Модель подбирается или заказывается, а не лепится кодом.** Кодом строится
только то, что меняется на лету: воксели, процедурные меши. Перед заказом
сверить папку назначения и бесплатные библиотеки (CC0): Kenney, Quaternius,
Poly Haven. Платная модель или генератор — только после «да» владельца
окном, с ценой.

## Паспорт

| Поле | Значение |
|---|---|
| исполнитель | вручную |
| источник | {{библиотеки CC0 (Kenney, Quaternius, Poly Haven); генератор, магазин, Blender — чем пользуется владелец}} |
| куда сохранять | `{{assets/models/…}}` — в заказе всегда полный путь файла |
| формат | glTF 2.0, `.glb` — один файл, текстуры внутри |
| файлы заказов | `docs/prompts/model3d-NN.md` |
| образцы к заказу | 2–3 принятые модели рядом по смыслу; образец вида — паспорт темы в `docs/refs/` |
| тариф и права | {{библиотеки и их лицензии; генератор или магазин — тариф и разрешает ли он продавать, с какой даты; идёт в `docs/orders/ledger.md`}} |

## 1. Стиль

{{стилизация по шкале `docs/refs/INDEX.md`; силуэт и пропорции; степень
детализации; материал: цвета вершин, одна текстура-палитра или PBR (заказ
`texture`). Писать то, что видно на кадре стенда рядом с капсулой 1,8 м.}}

## 2. Масштаб и оси

Из «Правил проекта» `CLAUDE.md`, не заново:

- в файле glTF — метры и Y вверх (так устроен формат); в проекте —
  {{1 единица = 1 м, Y вверх — или единицы и оси движка из «Правил проекта»}};
- рост персонажа {{1,8 м}} — размер модели в метрах называется относительно
  него (дверь, стул, дерево);
- опорная точка — внизу по центру: модель стоит на земле в нуле;
- «вперёд»: у glTF лицо модели смотрит в +Z; {{в Godot «вперёд» — −Z:
  разворот — в настройке импорта, не в коде}};
- масштаб и поворот применены в самой модели (в сцене — 1 и 0).

## 3. Бюджет и формат

| Подвид | Треугольников | Текстуры | LOD | Столкновения |
|---|---|---|---|---|
| {{персонаж}} | {{после замера}} | | | капсула |
| {{предмет}} | | | | коробка |
| {{здание, скала, дерево}} | | | | простые формы |

- Бюджеты треугольников и текстур — после замера на железе из «Окружения»
  `docs/TESTING.md`, не на глаз; до замера — `?? нужно подтверждение`.
- LOD — упрощённые копии модели для дали: {{нужны ли, сколько ступеней, с
  какого расстояния}}.
- Столкновения — простыми формами (коробка, капсула, выпуклая оболочка), не по
  сетке модели.
- Анимации: {{нет | скелет и имена клипов}}.
- Имена файлов: {{латиницей, без пробелов}}.

## 4. Постоянная часть заказа

```
<модель>

Стиль: {{…}}.
glTF 2.0 (.glb), текстуры внутри. 1 единица = 1 м, Y вверх, лицо модели
смотрит в +Z, опорная точка внизу по центру, масштаб и поворот применены.
Не больше {{N}} треугольников, текстуры не больше {{…}} px.
Одна модель, без пола, фона, подставки и освещения.
```

В `<модель>` — что это, размер в метрах рядом с персонажем, из чего собрано и
чем отличается от соседней модели.

## 5. Как вести заказ

- Сначала библиотеки CC0: нашлось — в заказе ссылка и лицензия, заказывать
  нечего.
- Платное (магазин, генератор по подписке) — окно «да» с ценой до заказа.
- Генератору прикладывать образец вида и 2–3 принятые модели: иначе стиль
  плывёт.

## 6. Проверка перед отправкой

- размер в метрах и масштаб по росту персонажа;
- формат, оси, «вперёд», опорная точка;
- бюджет треугольников и текстур, LOD, столкновения;
- источник и лицензия, если модель готовая;
- назван **полный путь файла**.

## 7. Приёмка (`/add`)

Импорт движком без ошибок; снимок на стенде вида рядом с капсулой 1,8 м и
кубом 1 м (нет стенда — снимок из «Команд проверки»); стоит на земле, смотрит
«вперёд» в игре; треугольники и текстуры в бюджете; столкновения простые;
права — строкой в `docs/orders/ledger.md`. Затем «после правки ресурсов» из
`docs/TESTING.md`.

## 8. Лежит, но не используется

| Файл | Почему |
|---|---|
| | |
