# Заказ: графика (`art`)

**Главное правило: графика заказывается промтом, не рисуется кодом.** Кодом
делается только то, что анимируется и меняется на лету. Перед заказом сверить
папку назначения — там могло быть уже нарисовано.

## Паспорт

| Поле | Значение |
|---|---|
| исполнитель | {{вручную \| api}} |
| генератор | {{ChatGPT, Midjourney, … — чем пользуется владелец}} |
| куда сохранять | `{{assets/…}}` — в заказе всегда полный путь файла |
| формат | PNG |
| файлы заказов | `docs/prompts/art-NN.md` |
| образцы к заказу | 2–3 готовых файла, ближайших по смыслу, из папки назначения |

## 1. Стиль

{{техника (пиксель-арт, вектор, 3D-рендер, акварель…), тон, свет, степень
детализации, ощущение. Писать то, что можно проверить глазами по картинке.}}

## 2. Палитра

| Роль | Цвет | Где задан |
|---|---|---|
| | | |

{{цвета сторон, фракций, разделов — если у продукта они есть}}

## 3. Ракурс и композиция

{{вид (сверху, сбоку, изометрия, три четверти), куда «смотрит» объект и почему
— если этого требует код, назвать файл и строку. Сколько объектов в кадре, где
центр.}}

## 4. Размеры и формат

| Подвид | Холст | Фон | Примечание |
|---|---|---|---|
| {{объект}} | | | |
| {{фон, баннер}} | | | |
| {{значок, элемент интерфейса}} | | | |
| {{портрет}} | | | |

{{как вырезается фон (альфа, ключ по цвету) и что из-за этого запрещено внутри
объекта; мип-карты и читаемость при уменьшении; что тянется и режется на
части и какие места поэтому обязаны быть однородными; замеры содержимого,
которые снимаются с готового файла при подключении.}}

## 5. Постоянная часть промта

Постоянная часть не меняется от заказа к заказу. Меняется только `<объект>` —
одно-два предложения о том, что именно нарисовать, из чего собрано и чем
отличается от соседнего.

### {{подвид}}

```
<объект>

Стиль: {{…}}
{{ракурс}}
{{сколько объектов, где, на каком фоне}}
Без текста, подписей, водяных знаков и рамок.
```

{{обязательные слова в <объект> для этого подвида}}

## 6. Как вести заказ

- **Вручную — один непрерывный чат** на все партии: иначе стиль плывёт.
- **Прикладывать готовые файлы как образец** — лучше всего те, что рядом по
  смыслу. При исполнителе `api` это единственное, что держит стиль: каждый
  запрос там сам по себе.
- **Один объект за запрос.**

## 7. Проверка перед отправкой

- в промте назван размер холста и фон;
- {{обязательные слова подвидов}};
- никакого текста, подписей, рамок и водяных знаков;
- один объект в кадре;
- назван **полный путь файла**.

## 8. Приёмка (`/add`)

Размер холста, альфа или ожидаемый фон, читаемость на тёмном и светлом, в
уменьшенном виде; {{замеры содержимого}}; затем «после правки ресурсов» из
`docs/TESTING.md`.

## 9. Лежит, но не используется

| Файл | Почему |
|---|---|
| | |
