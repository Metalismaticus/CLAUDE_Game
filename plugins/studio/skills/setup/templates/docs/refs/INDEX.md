# Образцы вида и ощущения

Как должна выглядеть и ощущаться игра — картинками и числами, а не
прилагательными. Здесь — общее для всех тем; по теме — паспорт
`docs/refs/<тема>.md`, её файлы — в папке `docs/refs/<тема>/`. Паспорт в
состоянии `черновик` пишет разбор образца (агент `reference`, зовут `/idea` и
`/setup`); подтверждает владелец — словами или окном «что вам нравится»; выбор
по листу сравнения (`[вид]`) или листу вариантов (`[ощущение]`) ложится в
«Журнал» паспорта. Пункт `[вид]` или `[ощущение]` по теме без подтверждённого
паспорта стоит `[ждёт образца]`. Концепт стиля — вид всей игры (картинка кадра,
ТЗ на стиль) — лежит в `docs/refs/_concept/` и раскладывается по паспортам тем и
«Правилам стиля» (`/idea`, «Концепт стиля»); замысел, механики и этапы он не
меняет.

## Главный образец

Не выбран.

<!-- Игра, мод, фильм или картинка, на которую проект равняется в целом:
файл, откуда, почему главный — словами владельца; концепт стиля —
`_concept/target-<дата>-<n>.<ext>` и его ТЗ `_concept/brief-<дата>.md`.
Выбирает владелец окном (`/setup`, раунд образцов, или `/idea`); агент сам не
ставит. -->

## Шкала стилизации

Не выбрана: мультяшно / стилизованно / полуреализм / реализм.

<!-- Выбор владельца и его слова дословно, например: «Стилизованно — „как в
Valheim, но ярче“ (2026-09-25)». Из ТЗ концепта — с источником «ТЗ <дата>,
п. N». -->

## Правила стиля

Не заданы.

<!-- Общее для всех тем, а не для одной, — по строке с источником: «ТЗ
<дата>, п. N» (`_concept/brief-<дата>.md`) или слова владельца с датой.
- Концепт <дата>: `_concept/target-<дата>-<n>.<ext>` (ТЗ
  `_concept/brief-<дата>.md`); правила не для всех тем — «для тем: …».
- Название стиля и формула вида (жанр, механики, тон мира — не сюда: это
  замысел): …
- Палитра: … (HEX из листа распознанного)
- Свет и время суток (как выглядят, не система): …
- Контраст природы и рукотворного: …
- Форма и детализация: …
- Ориентиры (не копировать): …
- Техника: …
- Настроение (для тем: …; тон мира — в замысле): …
- Шкала стилизации — в своём разделе выше.
- **Запреты:** по строке на запрет — …
Пишет `/idea` после окон концепта; расхождение паспорта темы с правилом — в
«Противоречия между темами». Запреты в `CLAUDE.md` не копируются: в его
«Правилах проекта» — строка-ссылка сюда; исполнитель `[вид]` читает их
здесь. -->

## Темы

| Тема | Паспорт | Состояние | Главное для владельца |
|---|---|---|---|

Тема — деревья, трава, вода, небо и свет, местность, постройки, персонаж,
эффекты, интерфейс-стиль, прыжок, камера, звук шагов, механика «урон от
падения» …; состояние — из паспорта: `черновик` · `подтверждён владельцем
<дата>` · `принят кадр <дата>`; главное — из слов владельца или окна, иначе
`не спрошено`.

## Противоречия между темами

Нет.

<!-- Что у тем не сходится (свет деревьев и воды из разных игр, разная
стилизация, прыжок из одной игры, а камера из другой) и что решено. -->

## Файлы

Имена в папке темы:

- `owner-<дата>-<n>.<ext>` — снимки и скрины владельца;
- `ref-<игра>-<n>.<ext>` — найденные кадры (jpg, png, webp; URL — в паспорте);
  видео образца — только ссылкой с моментом в паспорте, на диск не качать;
- `target-<n>.<ext>` — кадр-цель, в т. ч. перерисовка (заказ `art`, подвид
  «кадр-цель»): направление, а не пиксельная цель;
- `accepted-<дата>.<ext>` — принятый кадр нашей игры;
- `sheet-<дата>.png` — листы сравнения `[вид]` (`tools/look_sheet.py`);
- `sheet-<дата>.md` — листы вариантов `[ощущение]`: вариант — чем отличается —
  числа, как запустить, клавиши, что попробовать;
- `variant-<дата>-<буква>.wav` — звук варианта `[ощущение]`, чтобы послушать
  вне игры.

Папка `_concept/` — не тема, паспорта у неё нет: `target-<дата>-<n>.<ext>` —
картинка концепта; `brief-<дата>.md` — ТЗ дословно (вход, а не решение);
`target-<тема>-<n>.png` — вырезка темы, в паспорте — полным путём
`docs/refs/_concept/…`; `sheet-<дата>.png` — лист распознанного (вырезки тем
с номерами).

Длинная сторона картинки — не больше 2560 px, у листов `sheet-` — до 2576. Все
ли файлы на месте и не держит ли черновик пункт `[вид]` или `[ощущение]` —
`python tools/refs_check.py` (код 0 — да). Первый кадр `/setup` — в
`docs/refs/setup/` («Окружение» `docs/TESTING.md`).

Образцы лежат в git, если репозиторий приватный. Репозиторий: не проверено.
Публичный — чужие кадры `ref-…` в `.gitignore`, в паспорте — ссылка на
источник.
