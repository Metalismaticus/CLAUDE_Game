---
name: setup
description: Развернуть в папке проекта процесс studio — узнать замысел окнами, проверить компьютер и поставить недостающие программы после «да», разобрать образцы владельца в docs/refs/, создать CLAUDE.md и docs/, поставить очередь вертикальным срезом, для игры на движке — создать каркас и показать живое окно игры. Работает в пустой папке и в проекте с кодом; /setup обновить переносит в развёрнутый проект новый шаблон процесса. Звать по команде /setup.
argument-hint: "[что за проект | обновить]"
disable-model-invocation: true
---

Что разворачиваем: **$ARGUMENTS**

(Если строка выше пуста или осталась без подстановки — взять из сообщения
владельца; нет и там — начать с шага 1. «Обновить» — `steps/update.md`.)

Всё нужное — от базовой папки скилла, названной при вызове (не названа —
`${CLAUDE_PLUGIN_ROOT}/skills/setup`; нет и её — найти поиском
`skills/setup/SKILL.md` в папке плагинов):

- `steps/<шаг>.md` — подробности шага. **Прочитать файл шага, дойдя до
  него**, а не действовать по памяти; после сжатия контекста — перечитать.
- `templates/` — шаблоны документов и `tools/`. `{{…}}` — место для
  содержимого: в готовом документе не остаётся ни одной `{{`.
- `../../reference/ASKING.md` — как спрашивать; `../../tools/env_check.ps1` —
  проверка компьютера; `../../hooks/selftest.py` — самопроверка стража.

## Главное

- **Вопросы — окнами** по `ASKING.md`, только о решениях владельца. Железо,
  версии, что установлено, где движок — **факты**: узнать `env_check.ps1`, не
  спрашивать и не брать из памяти. Сказанное с оговоркой («наверное») —
  пометка `[предварительно]`, а не решение владельца.
- **Показывать, а не описывать.** Образцы владельца (игры, скрины, ссылки) до
  вопросов разбирает агент `reference` в `docs/refs/`; картинку — путём к
  файлу или `SendUserFile`, потом окно.
- **Программы ставить только после «да»** — вопрос на каждую программу.
- **Ничего существующего не перезаписывать** (кроме процессных разделов в
  «обновить» после согласия). До «Принять» на шаге 7 пишутся только
  `docs/SETUP-PLAN.md`, `docs/refs/`, `docs/engine-notes.md` и `tools/` из
  шаблонов.
- **Кода продукта не писать.** Каркас проекта движка на шаге 10 —
  инфраструктура; дальше код — только через очередь.
- **Журнал старта:** каждое решение и допущение — строкой (до шага 8 — в
  `docs/SETUP-PLAN.md`, потом в `docs/DECISIONS.md`, `## Старт`). Повторный
  `/setup` читает журнал и показывает, что меняется, а не переписывает
  документы.
- **Git** — файлы поимённо, никогда `git add -A` или `git add .`; отправлять —
  только если владелец сказал, и в тот репозиторий, который он назвал.
- Отчёты и вопросы — словами продукта.

## 0. Где мы

| Что в папке (сверху вниз) | Режим |
|---|---|
| есть `docs/SETUP-PLAN.md` | **прерванный старт** — прочитать план и `docs/refs/`, продолжить с первого несделанного шага: по журналу плана и файлам (есть `{{` в документах — шаг 8; нет очереди — 9; нет `docs/refs/setup/first-frame.png` и строки «Первый запуск окна» — 10; нет коммита — 12). План «было → станет» — прерванный пересмотр, `steps/update.md` |
| есть `docs/ROADMAP.md` | **развёрнут** — `steps/update.md` |
| есть код | **существующий** — сначала `steps/existing.md`, дальше шаги 1–12 с его поправками |
| пусто или только заметки | **новый проект** — шаги 1–12 |

Свой `CLAUDE.md` без метки studio — не заменять: окно «Вставить разделы
процесса (Recommended) / Показать разницу / Не трогать», разница — во
временный файл вне проекта.

## Шаги

1. **Окно из двух вопросов** — `steps/talk.md`: «Где вы сейчас?» (Идеи нет /
   Идея смутная / Идея ясная / Уже есть наработки) и «Как стартуем?»
   (Быстрый старт (Recommended) / Подробно).
2. **Одна просьба текстом**, не окном: «Расскажите своими словами и принесите
   всё, что есть: игры, на которые похоже, скрины (можно вставить в чат),
   ссылки, заметки». Ответ — дословно в `docs/SETUP-PLAN.md`.
3. **Проверка компьютера** — `steps/env.md`: `env_check.ps1`; недостающее для
   выбранного пути — окно «Поставить через winget (Recommended) / Покажу, где
   лежит / Пропустить». Версия движка — из установленного; новее твоих знаний
   или стек незнаком — `docs/engine-notes.md` и окно про Context7 (коннектор;
   подключает владелец). Итог — `## Окружение` в `docs/TESTING.md`, путь к движку —
   строкой в `CLAUDE.md`.
4. **Разбор до вопросов** — `steps/refs.md`: на каждый названный образец
   (≤ 3) агент `reference`, параллельно, и общий исследователь ловушек жанра
   и движка этой версии — в фоне, пока идут раунды (а) и (б).
5. **Раунды окон** — `steps/talk.md`: (а) жанр, камера, платформы, размер;
   (б) 30-секундный цикл; (в) образцы; (г) стек; (д) заказы, приватность,
   параллельность. В окне ≤ 4 вопроса; быстрый старт — ≤ 4 раундов,
   подробный — ≤ 7. После (г) — установка движка, `steps/env.md` п. 3–4.
6. **3D** — `steps/3d.md`: «Правила проекта» масштаба, осей и импорта; виды
   заказов `model3d`, `texture`.
7. **Пересказ** — `steps/plan.md` (проверки продумать до него —
   `steps/testing.md`): `docs/SETUP-PLAN.md`, путь владельцу, окно «Принять
   (Recommended) / Поправить понимание / Поправить очередь / Ещё поговорить».
   Кроме «Принять» — поправить и спросить снова.
8. **Файлы** — `steps/plan.md`: документы из шаблонов, `docs/refs/`,
   `.gitattributes`, `.gitignore`; быстрый старт — одностраничный бриф в
   `CONCEPT.md`, остальное — заготовками.
9. **Очередь — вертикальный срез** — `steps/plan.md`: проба стека (если
   нужна) → каркас и быстрая проверка → проверки и стенд вида → серая
   коробка со сценарием через ввод (назван образец управления — следом
   `[ощущение]`) → первый кадр по образцу `[вид]` → один полный цикл на 3–5
   минут.
10. **Каркас и живое окно** — `steps/finale.md`: проект движка его же
    инструментами, запуск окна, снимок `docs/refs/setup/first-frame.png`,
    окно «Видите окно игры?».
11. **Журнал старта** — `steps/plan.md`: `docs/DECISIONS.md`, `## Старт` —
    каждое решение и допущение плана и итог шага 10, по строке.
12. **Коммит, страж, отчёт** — ниже.

## 12. Коммит, страж, отчёт

Нет репозитория — создать на ветке `main` без окна, строкой «Решено за вас»:
без истории не работают контрольные коммиты и откат `/done`. Фоновый сбор
`docs/engine-notes.md` (шаг 3) — дождаться до коммита. Первый коммит — файлы
**поимённо**; в существующем проекте — только созданные `/setup`.
`docs/SETUP-PLAN.md` не коммитить: после коммита удалить, его содержимое уже в
документах. Отправлять — только по слову владельца.

Самопроверка хуков: `python -X utf8 "<базовая папка скилла>/../../hooks/selftest.py"`,
в контекст только итог. Код 0 — «страж коммитов: работает». Иначе или нет
`python` — «страж коммитов: молчит» и первая строка провала: в папке двух
чатов ничто не остановит `git add -A` и `git stash`.

Отчёт:

- что создано и что намеренно не тронуто;
- окружение: что стоит, что поставлено сейчас, чего нет и что без этого не
  заработает;
- что запущено: окно игры и путь снимка — или прямо «не запустилось:
  <почему>, <что дальше>»;
- как теперь проверяется продукт и что работает уже сегодня;
- строка «страж коммитов: работает | молчит»;
- открытые решения — списком, как в `/need`;
- как работать дальше: **два чата в этой папке** — назвать их «<Проект> ·
  замысел» (`/idea`, `/fault`, `/order`, `/need`) и «<Проект> · разработка»
  (`/start`, `/done`, `/add`, `/check`, `/parallel`, `/retro`, `/release`);
  `/board` — в любом. Первый шаг: в чате разработки `/start` — он возьмёт
  первый пункт очереди.
